// A basic page that will print a list of all rountes in the bench folder
// This is a NEXTJS app using the pages folder to create routes

import React from "react";

export default function Benches() {
  return <div>all a list of pages here</div>;
}
